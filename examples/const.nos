// Define a constant.
const test = 10

// Print the value of the constant.
put(test)

// Work with the constant
x = 7
put(x + test)
x = 5
put(x + test)

// Work with several constants.
const foo = "Hello, "
const bar = "world!"
put(foo <> bar)